{
  "name": "domain-event-driven",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "format": "npx prettier --write 'src/**/*.ts'",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^4.5.4",
    "sass": "^1.51.0",
    "vite": "^2.9.5"
  }
}
